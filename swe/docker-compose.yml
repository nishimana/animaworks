services:
  swe:
    build:
      context: ..
      dockerfile: swe/Dockerfile
    network_mode: host
    environment:
      - ANTHROPIC_API_KEY
      - OPENAI_API_KEY
    volumes:
      - ./results:/app/swe/results
    working_dir: /app
    command: ["--setup-team", "--test"]
