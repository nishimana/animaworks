# 2026-03-02 Activity Log

## 09:00 Security PR Progress

Continuing work on 3 security PRs (CSP/CORS/API rate limiting). CSP (PR #8) merged.

## 10:00 Security Knowledge Documentation

Systematized the security findings from the v0.3 audit into knowledge/security-best-practices.md. Covered four areas across 250 lines: CSP header implementation, CORS configuration, XSS protection, and API rate limiting.

## 13:00 [EMERGENCY] Tenant Isolation Bug

Nova reported that "customers can view other users' designs."
Immediately started code investigation. Used `grep -r 'setTenant\|setTeamId' src/api/` to audit all tenant configuration points.

**Root cause identified (15 min)**: Cache key in design-cache.ts was missing the tenant ID.
Fixed `cache.get(designId)` → `cache.get(tenantId:designId)`.

Created PR #11, all tests pass. Reported to Alex and suggested Nova handle customer notifications.

## 14:00 Hotfix Deployed → Knowledge Recorded

Fix deployed. Recorded lessons in knowledge/tenant-isolation-design.md:
"Cache keys must always include the tenant ID. Added to the code review checklist."

## 17:00 All Security Items Complete

CORS (#9) and API rate limiting (#10) also merged. All 3 security fixes complete. All tests pass.

[REFLECTION]
The tenant isolation bug was resolved within 30 minutes from discovery to fix. The approach of using grep to audit all relevant locations before applying the fix was effective. However, the fact that this cache key design issue was missed during the original code review is a point for improvement. Going forward, "Does the cache key include the tenant ID?" has been added to the PR review checklist.
[/REFLECTION]
