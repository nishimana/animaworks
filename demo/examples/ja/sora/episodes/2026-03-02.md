# 2026-03-02 行動ログ

## 09:00 セキュリティPR進捗

セキュリティ3件(CSP/CORS/APIレート制限)のPR対応を継続。CSP (PR #8) がマージ完了。

## 10:00 セキュリティナレッジ作成

v0.3のセキュリティ修正で得た知見をknowledge/security-best-practices.mdに体系化。CSPヘッダー実装、CORS設定、XSS対策、APIレート制限の4領域を250行で記述。

## 13:00 【緊急】テナント分離バグ発生

Hinaから「顧客が他ユーザーのデザインを閲覧できる」との報告。
即座にコード調査を開始。`grep -r 'setTenant\|setTeamId' src/api/` で全テナント設定箇所を洗い出し。

**根本原因特定（15分）**: design-cache.ts のキャッシュキーにテナントIDが含まれていなかった。
`cache.get(designId)` → `cache.get(tenantId:designId)` に修正。

PR #11 作成、テスト全パス、Kaitoに報告。Hinaへの顧客通知も提案。

## 14:00 ホットフィックスデプロイ → ナレッジ記録

修正デプロイ完了。教訓をknowledge/tenant-isolation-design.mdに記録:
「キャッシュキーには必ずテナントIDを含める。コードレビューチェックリストに追加する。」

## 17:00 セキュリティ全件完了

CORS (#9) とAPIレート制限 (#10) もマージ完了。セキュリティ修正3件全て完了。テスト全パス。

[REFLECTION]
テナント分離バグは発見から修正まで30分で対応できた。grepで全箇所を洗い出してから修正する手法が効いた。しかし、そもそもキャッシュキーの設計レビューでこのパターンを見逃していたのは反省点。今後はPRレビューのチェックリストに「キャッシュキーにテナントIDを含むか」を追加した。
[/REFLECTION]
